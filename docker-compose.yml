version: "3.2"

services:
  web:
    build: .
    container_name: amoozande
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - type: bind
        source: .
        target: /usr/src/app
    ports:
      - 8000:8000